---
name: "OPSX: Resume"
description: 会话恢复 — 自动读取活跃变更进度，无需用户重新描述上下文
category: Workflow
tags: [workflow, resume, session]
---

当用户说 `/opsx:resume`、"继续上次的工作"、"接着做"、"从上次断点继续" 时执行此命令。

**执行步骤**

1. **扫描活跃变更**

   读取 `openspec/changes/` 目录，列出所有**非 archive** 的子目录，找出含有 `proposal.md` 或 `tasks.md` 的目录（即活跃变更）。

   - **零个活跃变更**：告知用户当前没有进行中的变更，建议运行 `/opsx:propose` 创建新变更
   - **一个活跃变更**：直接加载，继续第二步
   - **多个活跃变更**：展示列表，请用户选择要恢复哪个，然后继续第二步

2. **加载变更上下文**

   读取该变更目录下的所有工件：
   - `proposal.md` — 了解变更目标和边界
   - `design.md` — 了解技术设计决策
   - `tasks.md` — 获取任务清单和当前进度

   同时读取：
   - `MEMORY.md`（如果存在）— 了解项目历史踩坑记录
   - `docs/architecture/implicit-contracts.md` — 确保续做符合隐性约定

3. **统计并展示进度**

   解析 `tasks.md` 中的复选框：
   - `- [x]` → 已完成
   - `- [ ]` → 待完成

   以清晰格式展示恢复摘要：

   ```
   📋 恢复变更：<change-name>

   进度：N/M 任务已完成（X%）

   ✅ 已完成：
     [1.1] 任务描述
     ...

   ⏳ 待完成：
     [2.1] 任务描述
     ...

   下一个任务：[X.X] <第一个未完成任务描述>
   ```

4. **询问确认并开始**

   询问用户：

   > "是否立即从第一个未完成任务开始？（直接回复"是"或"继续"即可）"

   用户确认后，**直接开始执行第一个未完成的任务**，无需等待进一步指令。执行方式与 `/opsx:apply` 相同：严格按照 `tasks.md` 的任务顺序，逐条完成并用 `[x]` 标记。

**特殊情况处理**

- 如果 `tasks.md` 中所有任务都已完成（全部 `[x]`）：提示用户可以运行 `/opsx:archive` 归档
- 不要自行扩展任务范围，只做 `tasks.md` 中明确列出的内容
- 每完成一个里程碑后执行：`mvn -q -DskipTests compile`
